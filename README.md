My personal portfolio for showcasing my work and writing.

Built on Astro.

To run, clone the repository: git clone `https://github.com/josh-billington/portfolio.git`

In the project root run: `npm install`

Then npm run dev to start the local development server at: `localhost:4321`

The project is setup with dependabot to automatically scan the dependency graph for updates and vulnerabilities.